# 있는대로 (fridge-chef)

냉장고에 있는 재료로 만들 수 있는 요리를 추천하고, 부족한 재료만 장보기 리스트로 정리해주는 웹서비스.
1인 가구 자취생이 주 사용자.

**스택**: 프레임워크 없는 순수 HTML/CSS/JS (모듈 아님, `<script src="js/...">` 방식).
백엔드는 Vercel Serverless Functions(Python)이고 OpenAI API로 추천을 생성한다. 빌드 스텝 없음.

**페이지**: `index.html`(홈), `pantry.html`(냉장고), `recipe.html`(레시피 추천), `mealplan.html`(식단 계획), `shopping.html`(장보기).
전부 `css/style.css`를 공유하고, 각자 `js/` 아래 자기 스크립트(`main.js`, `recipe.js`, `mealplan.js`, `shopping.js`)와 공용 `js/shared.js`를 쓴다.

---

## 브랜드

| 항목 | 값 |
|---|---|
| 브랜드명 | **있는대로** (붙여씀) |
| 슬로건 | **냉장고에 있는 대로, 오늘 한끼 완성** |
| 이전 이름 | ~~냉털 셰프 / Fridge Chef~~ — 코드·UI·메타데이터에서 전부 제거할 것 (`index.html`의 `<title>`, `.brand` 링크 텍스트 `🧊 냉털 셰프`, `.hero-note`의 "냉털은 냉장고 털기의 줄임말이에요" 포함) |

### 표기 규칙 (반드시 지킬 것)

- 브랜드명은 **붙여쓴다**: `있는대로`
- 문장 안에서는 **띄어쓴다**: `냉장고에 있는 대로` — '대로'가 의존명사이므로 맞춤법상 띄어쓰기가 맞다
- 이 둘을 혼동해서 `있는 대로`를 브랜드명으로, `있는대로`를 문장 안에 쓰지 않는다

### 포지셔닝

**기술이 아니라 안심을 판다.** "AI가 추천"은 기능 설명이고, "있는 대로 해먹어도 충분하다"는 약속이다.
AI는 구현 방법이므로 화면 앞단에 내세우지 않는다. 히어로·버튼·빈 상태 어디에도 "AI" 단어를 쓰지 않는다.
(FAQ의 "어떤 기술로 만들었나요?" 답변처럼 기술 스택을 설명하는 문맥은 예외 — 사용자에게 보여주는 기능 문구에만 적용)

---

## 보이스 & 톤

자취 선배나 룸메이트가 "그냥 있는 걸로 해먹어도 충분해"라고 말해주는 다정하고 편한 톤.
문체는 **해요체**로 통일한다. 완벽하지 않아도 괜찮다는 위로가 깔려 있어야 한다.

### 이렇게 쓴다

- "애매하게 남은 재료만 적어주세요"
- "두세 개만 있어도 충분해요"
- "이것만 사면 돼요"
- "다 있어요! 바로 해먹을 수 있어요"

### 이렇게 쓰지 않는다

- "AI가 최적의 레시피를 분석합니다" — 기술 자랑
- "재료를 입력하십시오" — 딱딱한 하십시오체
- "추천 결과가 존재하지 않습니다" — 시스템 말투
- "강력한 AI 기반 식단 솔루션" — 과장된 마케팅 문구

**모든 UI 문구는 `js/copy.js`의 전역 `COPY` 객체에서 가져온다.** HTML/JS에 문자열을 하드코딩하지 않는다.
새 문구가 필요하면 `copy.js`에 먼저 추가한다. 모듈이 아니므로 `copy.js`를 각 페이지에서
**다른 스크립트보다 먼저** `<script>`로 불러와야 `COPY`가 전역으로 잡힌다.

```html
<script src="js/copy.js"></script>
<script src="js/shared.js"></script>
<script src="js/main.js"></script>
```

---

## 디자인 시스템

**모든 색·간격·타이포는 `css/tokens.css`의 CSS 변수만 참조한다.**
`css/style.css`나 인라인 스타일에 hex 값이나 임의의 px 값을 직접 쓰지 않는다.

`tokens.css`는 각 HTML의 `<head>`에서 `style.css`보다 **먼저** 링크한다.

```html
<link rel="stylesheet" href="css/tokens.css" />
<link rel="stylesheet" href="css/style.css" />
```

### 색

시맨틱 변수만 쓴다: `--bg` `--surface` `--border` `--text` `--text-muted` `--brand` `--accent-strong` 등.
`--green-600` 같은 원시 변수는 tokens.css 안에서만 참조한다.

핵심 규칙:

- **순백(`#FFF`)과 순검정(`#000`)을 쓰지 않는다.** 배경은 아이보리, 텍스트는 따뜻한 먹색.
- 강조색은 **감빛 하나만** 주요 CTA에 쓴다. 여러 색을 동시에 강조하지 않는다.
- 흰 글씨 버튼은 `--accent-strong`(5.11:1)에만 쓴다.
  `--accent`(3.69:1)는 대비 기준 미달이므로 아이콘 배경이나 18px 이상 굵은 글씨에만 허용.
- 다크모드는 `:root[data-theme="dark"]`로 전환한다. 시맨틱 변수를 쓰면 자동으로 따라온다.
  기존 `#theme-toggle` 버튼(🌙)의 토글 로직은 그대로 두고, `data-theme` 속성만 여기 연결되게 배선한다.
- 다크모드에서 브랜드·강조색은 라이트보다 밝은 톤으로 바뀐다(`--brand`, `--accent` 등 tokens.css에서 이미 처리됨).
  채도 낮은 라이트용 원색을 다크 배경에 그대로 쓰지 않는다.
- 보더는 두 종류다. 카드 구분선 같은 **장식용**은 `--border`를 쓰고 대비 기준에서 자유롭다.
  입력창·드롭다운처럼 **경계를 식별해야 하는 상호작용 요소**는 반드시 `--border-strong`을 쓴다.
  `--border-strong`은 입력창이 `--bg`(페이지 배경) 위에 있든 `--surface`(카드) 위에 있든
  두 경우 모두 3:1 이상이 나오도록 맞춰져 있다(라이트 3.42~3.69:1, 다크 3.45~3.81:1).
  다른 배경색 위에 새로 놓을 때는 반드시 실측하고 넘어간다 — 값 하나가 두 배경 모두를 커버 못 하면 소용없다.

기존 `--color-*` 변수 체계를 시맨틱 토큰으로 옮길 때는 195회 넘게 쓰인 값을 눈으로 다 대조하기 어려우니,
**먼저 옛 변수 → 새 변수 매핑표를 만들어 보여주고, 확인받은 뒤에 일괄 치환한다.** 확인 없이 바로 치환하면
의미가 다르게 매핑된 경우를 놓치기 쉽다.

### 폰트

- 제목(`h1`, `.logo`/`.brand`): **마루 부리** (`--font-display`, 명조)
- `h2` 이하와 본문·UI 전체: **Pretendard** (`--font-sans`, 고딕)
- **명조는 24px 이상에만 쓴다.** 이 프로젝트의 `h2`는 17.6~19.2px(섹션 제목)라 명조 대상에서 제외했다.
  섹션 제목까지 명조로 하고 싶으면 `h2` font-size를 24px 이상으로 먼저 올려야 한다 — 크기 안 올리고
  명조만 적용하지 않는다. 버튼·입력창·라벨·칩·네비게이션은 크기와 무관하게 항상 고딕.
- **명조는 SemiBold(600) 이상만 쓴다.** ExtraLight·Light(200~300)는 다크모드에서 얇은 획이
  번져 보이는(halation) 문제가 있어 제목에 쓰지 않는다.
- 한글 조판: 제목 자간 `-.03em`, 본문 행간 `1.7`

각 HTML `<head>`에 웹폰트 두 줄을 `tokens.css` 링크보다 앞에 추가한다:

```html
<link rel="preconnect" href="https://cdn.jsdelivr.net" crossorigin>
<link rel="stylesheet" as="style" crossorigin
  href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css">
<link rel="stylesheet" as="style" crossorigin
  href="https://cdn.jsdelivr.net/gh/fonts-archive/MaruBuri/MaruBuri.css">
```

---

## 하지 말 것 (개인 프로젝트 티가 나는 요소)

- 보라~파랑 그라데이션 배경 — AI 데모의 가장 흔한 표식
- 이모지를 로고나 아이콘으로 쓰는 것 (`🧊`, `✨`, `🍳`) — 단색 SVG로 대체한다
- 제목 옆 반짝임 이모지, "AI가 똑똑하게" 류의 문구
- 모든 요소 가운데 정렬 — 본문과 카드 내용은 왼쪽 정렬
- 과한 pill 라운드(`border-radius: 999px`)를 카드나 버튼에 쓰는 것 — 카드 12px, 버튼 10px
- 짙은 검정 그림자 — 그림자는 갈색 기반 저불투명도(`rgba(60,45,30,.07)`)

## 반드시 할 것

- 간격은 4px 배수로 통일한다
- 버튼에 `:hover`, `:active`, `:focus-visible` 상태를 모두 만든다
- 포커스 링(`:focus-visible`)을 제거하지 않는다 — 접근성 필수
- **빈 상태·로딩·에러 화면에도 브랜드 말투를 쓴다.** 여기가 가장 티 나는 지점이다.
- 파비콘, OG 이미지, 페이지 타이틀을 실제 값으로 채운다 (5개 페이지 전부)
- `prefers-reduced-motion`을 존중한다
- `#theme-toggle`처럼 아이콘 하나만 있는 버튼은 **`color`를 명시적으로 지정한다.** 이모지로
  쓸 때는 안 보이던 문제가, SVG로 바꾸는 순간(`currentColor` 상속) 브라우저 기본 검정이 적용돼
  다크모드에서 버튼이 안 보이는 버그로 튀어나온다. SVG로 바꾸기 전에 먼저 고친다.

---

## 참고 파일

- `css/tokens.css` — 색·타이포·간격·그림자 토큰 (단일 진실 공급원)
- `js/copy.js` — 모든 UI 문구 (전역 `COPY` 객체)
