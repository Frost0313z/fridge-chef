# 개발·운영 안내

이 문서는 자동화 도구와 개발자가 프로젝트를 수정할 때 지켜야 할 최소 규칙입니다. 제품 설명은 `README.md`, 브랜드와 문체는 `CLAUDE.md`, 세부 동작은 `docs/`의 명세를 먼저 확인합니다.

## 기본 명령

```bash
python -m http.server 8000   # 정적 화면
vercel dev                   # API 포함 로컬 실행
python api/test_handlers.py  # Python 테스트
node js/test_amounts.js      # 브라우저 로직 테스트
bash scripts/deploy.sh       # 프로덕션 배포
```

## 구조와 상태

- 프레임워크와 번들러 없이 HTML, CSS, 바닐라 자바스크립트를 사용한다.
- API 진입점은 `api/index.py` 하나이며 기능별 `handle(payload)`로 분기한다.
- 공통 재료 정규화와 저장 로직은 `js/shared.js`에 둔다.
- 사용자 문구는 가능한 한 `js/copy.js`에서 관리한다.
- 색·간격·그림자·폰트는 `css/tokens.css`의 토큰을 우선 사용한다.
- 사용자 데이터는 서버가 아니라 `localStorage`에 저장된다. 이전 버전 데이터와 수동으로 변형된 값도 안전하게 처리한다.

## 레시피 인덱스와 배포

`api/recipes_index.json`은 원본 CSV에서 만든 로컬 산출물입니다.

- 원본 CSV와 인덱스는 공개 Git에 올리지 않는다.
- 인덱스 형식 버전은 `scripts/build_recipe_index.py`와 `api/recipe_match.py`에서 함께 바꾼다.
- 파일이 없거나 손상되면 서비스는 인공지능 추천으로 폴백한다.
- `vercel.json`과 `.vercelignore`는 `.gitignore`와 역할이 다르다. 배포에는 로컬 인덱스가 포함되어야 한다.
- 프로덕션 배포는 반드시 `scripts/deploy.sh`를 사용한다.

데이터 사용 범위와 재생성 방법은 `docs/spec-recipe-match.md`를 따릅니다.

## 깨뜨리면 안 되는 기능 약속

- “지금 바로 만들기”는 가진 재료 안에서만 제안하며 부족한 재료가 없어야 한다.
- “조금만 장보기”는 부족한 재료가 적은 결과를 우선한다.
- 실제 데이터 매칭 결과는 조리 순서를 생성하지 않고 원문 링크로 연결한다.
- 재료명, 수량, 단위 파싱을 바꾸면 Python API와 자바스크립트 계산 결과가 같은지 함께 검증한다.
- `hidden` 요소에 `display`를 직접 주는 CSS를 추가할 때는 숨김 동작이 깨지지 않는지 확인한다.

## 변경 검증

1. 관련 자동 테스트를 실행한다.
2. 화면 변경은 모바일·데스크톱과 라이트·다크 모드를 확인한다.
3. 핵심 흐름인 냉장고 → 추천 → 요리 완료 → 식단 → 장보기를 점검한다.
4. 검증 이미지는 일회성으로 보관하고, 사용자나 과제에서 증빙을 요구한 경우에만 저장소에 추가한다.
5. 문서에는 현재 동작만 남긴다. 작업 과정과 완료 기록은 Git 커밋으로 관리한다.

## 금지 사항

- API 키, 원본 데이터, 개인 백업 파일 커밋
- KADX 원문의 소개글·조리 순서 복제 또는 상업적 사용
- 기능 문구에서 서비스가 인공지능 자체인 것처럼 과장
- 동일 규칙을 여러 파일에 복사해 서로 다른 값으로 유지
